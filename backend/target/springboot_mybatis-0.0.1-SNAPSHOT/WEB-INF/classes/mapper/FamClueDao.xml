<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.save.dao.FamClueDao">

    <resultMap type="com.save.entity.FamClue" id="FamClueMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="taskId" column="task_id" jdbcType="INTEGER"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="content" column="content" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="FamClueMap">
        select
          id, task_id, create_time, content
        from save.fam_clue
        where id = #{id}
    </select>
    <!--通过taskid查询-->
    <select id="queryByTaskId" resultMap="FamClueMap">
        select
          id, task_id, create_time, content
        from save.fam_clue
        where task_id = #{taskId}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="FamClueMap">
        select
          id, task_id, create_time, content
        from save.fam_clue
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="FamClueMap">
        select
        id, task_id, create_time, content
        from save.fam_clue
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="taskId != null">
                and task_id = #{taskId}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="content != null and content != ''">
                and content = #{content}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into save.fam_clue(task_id, create_time, content)
        values (#{taskId}, #{createTime}, #{content})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into save.fam_clue(task_id, create_time, content)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.taskId}, #{entity.createTime}, #{entity.content})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into save.fam_clue(task_id, create_time, content)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.taskId}, #{entity.createTime}, #{entity.content})
        </foreach>
        on duplicate key update
        task_id = values(task_id) , create_time = values(create_time) , content = values(content)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update save.fam_clue
        <set>
            <if test="taskId != null">
                task_id = #{taskId},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="content != null and content != ''">
                content = #{content},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from save.fam_clue where id = #{id}
    </delete>

</mapper>